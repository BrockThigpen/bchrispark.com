language: node_js
node_js: node
branches:
  only:
    - main
cache:
  directories:
    - node_modules
env:
  - CI = false
script: 'true'
before_deploy:
  - npm install netlify-cli -g
  - npm run build
deploy:
  script: netlify deploy -s $NET_SITE_ID -t $NETLIFY_ACCESS_TOKEN -p --dir ./build
